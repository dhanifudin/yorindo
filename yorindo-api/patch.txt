  /events/check-slug:
    get:
      summary: Check if a slug is available
      operationId: checkEventSlug
      tags: [Events]
      security:
        - BearerAuth: []
      parameters:
        - name: slug
          in: query
          required: true
          schema:
            type: string
        - name: excludeId
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Slug availability
          content:
            application/json:
              schema:
                type: object
                required: [available]
                properties:
                  available:
                    type: boolean
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
