# SSL certificates — never commit private keys
nginx/certs/

# Local environment files — secrets live on VPS only
.env
.env.*
!.env.example
!.env.demo
!.env.preview

# Dependencies
node_modules/

# OS
.DS_Store

# Editor
.vscode/
.idea/

.qwen/settings*
