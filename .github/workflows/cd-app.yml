name: CD — App Preview

on:
  push:
    branches:
      - main
    paths:
      - 'yorindo-app/**'
  workflow_dispatch:

env:
  IMAGE_APP: ${{ secrets.DOCKERHUB_USERNAME }}/yorindo-app

jobs:
  build-and-push:
    name: Build & Push App Preview Image
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.tag.outputs.value }}
    steps:
      - uses: actions/checkout@v4

      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set image tag
        id: tag
        run: echo "value=preview-${GITHUB_SHA::8}" >> $GITHUB_OUTPUT

      - name: Build and push App preview image
        uses: docker/build-push-action@v5
        with:
          context: ./yorindo-app
          push: true
          tags: |
            ${{ env.IMAGE_APP }}:${{ steps.tag.outputs.value }}
            ${{ env.IMAGE_APP }}:preview
          build-args: |
            NEXT_PUBLIC_ENABLE_MOCKS=true
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy App Preview to VPS
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Prepare deploy env file
        env:
          SENTRY_DSN: ${{ secrets.APP_SENTRY_DSN }}
        run: |
          python - <<'PY'
          from pathlib import Path
          import os

          template = Path('env.app.template').read_text()
          sentry_dsn = os.environ.get('SENTRY_DSN', '')
          if sentry_dsn:
              template = template.replace(
                  '# NEXT_PUBLIC_SENTRY_DSN=https://<key>@o<org>.ingest.sentry.io/<project>',
                  f'NEXT_PUBLIC_SENTRY_DSN={sentry_dsn}'
              ).replace(
                  '# SENTRY_DSN=https://<key>@o<org>.ingest.sentry.io/<project>',
                  f'SENTRY_DSN={sentry_dsn}'
              )
          Path('.deploy.env').write_text(template)
          PY
          echo "IMAGE_TAG=${{ needs.build-and-push.outputs.image_tag }}" >> .deploy.env

      - name: Ensure VPS directory exists
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: mkdir -p /var/www/yorindo-app

      - name: Upload compose and env files to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          source: docker-compose.app.yml,.deploy.env
          target: /var/www/yorindo-app

      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            restore_previous_env() {
              if [ -f /var/www/yorindo-app/.env.previous ]; then
                cp /var/www/yorindo-app/.env.previous /var/www/yorindo-app/.env
              fi
            }

            test -s /var/www/yorindo-app/.deploy.env
            chmod 600 /var/www/yorindo-app/.deploy.env
            if ! grep -Rqs "127.0.0.1:5173" /etc/nginx/sites-enabled /etc/nginx/conf.d 2>/dev/null; then
              echo "VPS nginx is not configured to proxy port 5173. Configure the VPS nginx virtual host for app.dhanifudin.com before deploying."
              exit 1
            fi
            if [ -f /var/www/yorindo-app/.env ]; then
              cp /var/www/yorindo-app/.env /var/www/yorindo-app/.env.previous
            fi
            mv /var/www/yorindo-app/.deploy.env /var/www/yorindo-app/.env
            chmod 600 /var/www/yorindo-app/.env
            cd /var/www/yorindo-app
            if ! docker compose -f docker-compose.app.yml pull; then
              restore_previous_env
              exit 1
            fi
            if ! docker compose -f docker-compose.app.yml up -d; then
              restore_previous_env
              docker compose -f docker-compose.app.yml up -d
              exit 1
            fi

            sleep 15
            response=$(curl -s -o /dev/null -w "%{http_code}" https://app.dhanifudin.com)
            if [ "$response" != "200" ]; then
              restore_previous_env
              docker compose -f docker-compose.app.yml up -d
              echo "Health check failed: HTTP $response"
              exit 1
            fi
            echo "Health check passed: HTTP $response"
